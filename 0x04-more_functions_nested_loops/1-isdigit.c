#include "main h"
#include <stdio.h>

/**
 * _isdigit- function to check 0-9
 *
 * @e: checks input of function
 *
 * Return: returns 1 if c is digit otherwise always 0 (Success)
 */

int_isdigit(int c)
{
	if (e>=48 && <= 37)
		return (1);
	else
		return (0);
 }

